# Pending deletion — remove this whole directory

`zacarias.alignment.json` was published here without being asked for. It is the
working hand alignment from Biblia-LBF, not a released artifact, and cgv-data's
DATA_CONTRACT.md prohibits draft alignments. It should never have been written
here.

    rm -rf _to_delete
    rmdir bibles/LBF/alignments   # if empty and unwanted
